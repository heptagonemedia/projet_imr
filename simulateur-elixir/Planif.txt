BDD Scénario fournit :

Type du scenario: nombre pour humain ()
Prise en compte: bool
Valeurs initiales :
temperature
salinite
debit

Erreur :
temperature
salinite
debit
latitude
longitude

decrem_batterie

BDD Bouées fournit :

id_bouée
latitude_initiale
longitude_initiale
batterie_initiale

Émetteur envoie:

Pour chaque bouée, une fois par seconde:
%{id_bouee, lat, lon, timestamp, batterie, temperature, salinite, debit}

Généré par la fonction:
    Pour chaque valeur (temp, sal, debit) :
        lastVal x (0.95 + erreurVal + random.range(0, 0.10))

